- job:
    name: itrust
    project-type: pipeline
    auth-token: token
    dsl: |
        node {
            stage('Source') {
            } 
            stage('Build') {

            }
            stage('Start server and test') {
              sh 'echo "Done!"'
            }
        }